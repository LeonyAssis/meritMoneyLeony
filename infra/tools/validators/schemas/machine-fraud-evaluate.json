{
  "type": "object",
  "id": "machine-fraud-evaluate.json",
  "properties": {
    "status": {
      "type": "string",
      "enum": [
        "fraud",
        "rejected"
      ]
    },
    "reason": {
      "$ref": "defs.json#/definitions/longText"
    },
    "user_id": {
      "$ref": "defs.json#/definitions/positiveInt"
    }
  },
  "required": [
    "status",
    "reason",
    "user_id"
  ]
}
